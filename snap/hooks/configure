#!/bin/bash
set -e

port="$(snapctl get port)"
if [ -n "$port" ]; then
    if ! echo "$port" | grep -qE '^[0-9]+$' || [ "$port" -lt 1 ] || [ "$port" -gt 65535 ]; then
        echo "Invalid port: must be an integer between 1 and 65535" >&2
        exit 1
    fi
fi

snapctl restart charm-studio.api
